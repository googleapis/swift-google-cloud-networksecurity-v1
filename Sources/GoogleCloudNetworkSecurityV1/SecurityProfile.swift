// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// SecurityProfile is a resource that defines the behavior for one of many
/// ProfileTypes.
public struct SecurityProfile: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Immutable. Identifier. Name of the SecurityProfile resource. It matches
  /// pattern
  /// `projects|organizations/*/locations/{location}/securityProfiles/{security_profile}`.
  public var name: Swift.String = Swift.String()

  /// Optional. An optional description of the profile. Max length 512
  /// characters.
  public var description: Swift.String = Swift.String()

  /// Output only. Resource creation timestamp.
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Last resource update timestamp.
  public var updateTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. This checksum is computed by the server based on the value of
  /// other fields, and may be sent on update and delete requests to ensure the
  /// client has an up-to-date value before proceeding.
  public var etag: Swift.String = Swift.String()

  /// Optional. Labels as key value pairs.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Immutable. The single ProfileType that the SecurityProfile resource
  /// configures.
  public var type: SecurityProfile.ProfileType = SecurityProfile.ProfileType()

  /// The behavior for the ProfileType that the SecurityProfile resource is meant
  /// to configure. This field must correspond to the ProfileType of the
  /// SecurityProfile.
  public var profile: OneOf_Profile? = nil

  /// Initialize a new instance of `SecurityProfile`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = SecurityProfile().with { $0.threatPreventionProfile = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case threatPreventionProfile = "threatPreventionProfile"
    case customMirroringProfile = "customMirroringProfile"
    case customInterceptProfile = "customInterceptProfile"
    case urlFilteringProfile = "urlFilteringProfile"
    case name = "name"
    case description = "description"
    case createTime = "createTime"
    case updateTime = "updateTime"
    case etag = "etag"
    case labels = "labels"
    case type = "type"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.description = try container.decode(Swift.String.self, forKey: .description)
    self.createTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .createTime)
    self.updateTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .updateTime)
    self.etag = try container.decode(Swift.String.self, forKey: .etag)
    self.labels = try container.decode([Swift.String: Swift.String].self, forKey: .labels)
    self.type = try container.decode(SecurityProfile.ProfileType.self, forKey: .type)

    var profile: OneOf_Profile? = nil
    let profileCheckAndSet = {
      if profile != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'profile'"))
      }
      profile = $0
    }
    if let threatPreventionProfile = try container.decodeIfPresent(
      ThreatPreventionProfile?.self, forKey: .threatPreventionProfile)
    {
      try profileCheckAndSet(.threatPreventionProfile(threatPreventionProfile))
    }
    if let customMirroringProfile = try container.decodeIfPresent(
      CustomMirroringProfile?.self, forKey: .customMirroringProfile)
    {
      try profileCheckAndSet(.customMirroringProfile(customMirroringProfile))
    }
    if let customInterceptProfile = try container.decodeIfPresent(
      CustomInterceptProfile?.self, forKey: .customInterceptProfile)
    {
      try profileCheckAndSet(.customInterceptProfile(customInterceptProfile))
    }
    if let urlFilteringProfile = try container.decodeIfPresent(
      UrlFilteringProfile?.self, forKey: .urlFilteringProfile)
    {
      try profileCheckAndSet(.urlFilteringProfile(urlFilteringProfile))
    }
    self.profile = profile
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.description, forKey: .description)
    try container.encode(self.createTime, forKey: .createTime)
    try container.encode(self.updateTime, forKey: .updateTime)
    try container.encode(self.etag, forKey: .etag)
    try container.encode(self.labels, forKey: .labels)
    try container.encode(self.type, forKey: .type)

    if let choice = self.profile {
      switch choice {
      case .threatPreventionProfile(let value):
        try container.encode(value, forKey: .threatPreventionProfile)
      case .customMirroringProfile(let value):
        try container.encode(value, forKey: .customMirroringProfile)
      case .customInterceptProfile(let value):
        try container.encode(value, forKey: .customInterceptProfile)
      case .urlFilteringProfile(let value):
        try container.encode(value, forKey: .urlFilteringProfile)
      }
    }
  }

  /// The possible types that the SecurityProfile resource can configure.
  public enum ProfileType: Codable, Equatable, Sendable {
    /// Profile type not specified.
    case unspecified
    /// Profile type for threat prevention.
    case threatPrevention
    /// Profile type for packet mirroring v2
    case customMirroring
    /// Profile type for TPPI.
    case customIntercept
    /// Profile type for URL filtering.
    case urlFiltering
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .threatPrevention: return 1
      case .customMirroring: return 2
      case .customIntercept: return 3
      case .urlFiltering: return 5
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "PROFILE_TYPE_UNSPECIFIED"
      case .threatPrevention: return "THREAT_PREVENTION"
      case .customMirroring: return "CUSTOM_MIRRORING"
      case .customIntercept: return "CUSTOM_INTERCEPT"
      case .urlFiltering: return "URL_FILTERING"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ProfileType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "PROFILE_TYPE_UNSPECIFIED": self = .unspecified
      case "THREAT_PREVENTION": self = .threatPrevention
      case "CUSTOM_MIRRORING": self = .customMirroring
      case "CUSTOM_INTERCEPT": self = .customIntercept
      case "URL_FILTERING": self = .urlFiltering
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ProfileType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .threatPrevention
      case 2: self = .customMirroring
      case 3: self = .customIntercept
      case 5: self = .urlFiltering
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .threatPrevention: return try container.encode(1)
      case .customMirroring: return try container.encode(2)
      case .customIntercept: return try container.encode(3)
      case .urlFiltering: return try container.encode(5)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The behavior for the ProfileType that the SecurityProfile resource is meant
  /// to configure. This field must correspond to the ProfileType of the
  /// SecurityProfile.
  public enum OneOf_Profile: Codable, Equatable, Sendable {
    /// The threat prevention configuration for the SecurityProfile.
    indirect case threatPreventionProfile(ThreatPreventionProfile?)
    /// The custom Packet Mirroring v2 configuration for the SecurityProfile.
    indirect case customMirroringProfile(CustomMirroringProfile?)
    /// The custom TPPI configuration for the SecurityProfile.
    indirect case customInterceptProfile(CustomInterceptProfile?)
    /// The URL filtering configuration for the SecurityProfile.
    indirect case urlFilteringProfile(UrlFilteringProfile?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networksecurity.v1.SecurityProfile"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
