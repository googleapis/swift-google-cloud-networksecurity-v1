// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// The GatewaySecurityPolicyRule resource is in a nested collection within a
/// GatewaySecurityPolicy and represents a traffic matching condition and
/// associated action to perform.
public struct GatewaySecurityPolicyRule: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Required. Immutable. Name of the resource. ame is the full resource name so
  /// projects/{project}/locations/{location}/gatewaySecurityPolicies/{gateway_security_policy}/rules/{rule}
  /// rule should match the
  /// pattern: (^[a-z]([a-z0-9-]{0,61}[a-z0-9])?$).
  public var name: Swift.String = Swift.String()

  /// Output only. Time when the rule was created.
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. Time when the rule was updated.
  public var updateTime: GoogleCloudWkt.Timestamp? = nil

  /// Required. Whether the rule is enforced.
  public var enabled: Swift.Bool = Swift.Bool()

  /// Required. Priority of the rule.
  /// Lower number corresponds to higher precedence.
  public var priority: Swift.Int32 = Swift.Int32()

  /// Optional. Free-text description of the resource.
  public var description: Swift.String = Swift.String()

  /// Required. CEL expression for matching on session criteria.
  public var sessionMatcher: Swift.String = Swift.String()

  /// Optional. CEL expression for matching on L7/application level criteria.
  public var applicationMatcher: Swift.String = Swift.String()

  /// Optional. Flag to enable TLS inspection of traffic matching on
  /// <session_matcher>, can only be true if the parent GatewaySecurityPolicy
  /// references a TLSInspectionConfig.
  public var tlsInspectionEnabled: Swift.Bool = Swift.Bool()

  public var profile: OneOf_Profile? = nil

  /// Initialize a new instance of `GatewaySecurityPolicyRule`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = GatewaySecurityPolicyRule().with { $0.basicProfile = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case basicProfile = "basicProfile"
    case name = "name"
    case createTime = "createTime"
    case updateTime = "updateTime"
    case enabled = "enabled"
    case priority = "priority"
    case description = "description"
    case sessionMatcher = "sessionMatcher"
    case applicationMatcher = "applicationMatcher"
    case tlsInspectionEnabled = "tlsInspectionEnabled"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.createTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .createTime)
    self.updateTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .updateTime)
    self.enabled = try container.decode(Swift.Bool.self, forKey: .enabled)
    self.priority = try container.decode(Swift.Int32.self, forKey: .priority)
    self.description = try container.decode(Swift.String.self, forKey: .description)
    self.sessionMatcher = try container.decode(Swift.String.self, forKey: .sessionMatcher)
    self.applicationMatcher = try container.decode(Swift.String.self, forKey: .applicationMatcher)
    self.tlsInspectionEnabled = try container.decode(Swift.Bool.self, forKey: .tlsInspectionEnabled)

    var profile: OneOf_Profile? = nil
    let profileCheckAndSet = {
      if profile != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'profile'"))
      }
      profile = $0
    }
    if let basicProfile = try container.decodeIfPresent(
      GatewaySecurityPolicyRule.BasicProfile.self, forKey: .basicProfile)
    {
      try profileCheckAndSet(.basicProfile(basicProfile))
    }
    self.profile = profile
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.createTime, forKey: .createTime)
    try container.encode(self.updateTime, forKey: .updateTime)
    try container.encode(self.enabled, forKey: .enabled)
    try container.encode(self.priority, forKey: .priority)
    try container.encode(self.description, forKey: .description)
    try container.encode(self.sessionMatcher, forKey: .sessionMatcher)
    try container.encode(self.applicationMatcher, forKey: .applicationMatcher)
    try container.encode(self.tlsInspectionEnabled, forKey: .tlsInspectionEnabled)

    if let choice = self.profile {
      switch choice {
      case .basicProfile(let value):
        try container.encode(value, forKey: .basicProfile)
      }
    }
  }

  /// enum to define the primitive action.
  public enum BasicProfile: Codable, Equatable, Sendable {
    /// If there is not a mentioned action for the target.
    case unspecified
    /// Allow the matched traffic.
    case allow
    /// Deny the matched traffic.
    case deny
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .allow: return 1
      case .deny: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "BASIC_PROFILE_UNSPECIFIED"
      case .allow: return "ALLOW"
      case .deny: return "DENY"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:BasicProfile/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "BASIC_PROFILE_UNSPECIFIED": self = .unspecified
      case "ALLOW": self = .allow
      case "DENY": self = .deny
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:BasicProfile/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .allow
      case 2: self = .deny
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .allow: return try container.encode(1)
      case .deny: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public enum OneOf_Profile: Codable, Equatable, Sendable {
    /// Required. Profile which tells what the primitive action should be.
    case basicProfile(GatewaySecurityPolicyRule.BasicProfile)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networksecurity.v1.GatewaySecurityPolicyRule"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
