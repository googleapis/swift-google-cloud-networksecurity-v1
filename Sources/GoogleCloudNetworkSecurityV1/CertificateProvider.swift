// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// Specification of certificate provider. Defines the mechanism to obtain the
/// certificate and private key for peer to peer authentication.
public struct CertificateProvider: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// The type of certificate provider which provides the certificates and
  /// private keys.
  public var type: OneOf_Type? = nil

  /// Initialize a new instance of `CertificateProvider`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = CertificateProvider().with { $0.grpcEndpoint = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case grpcEndpoint = "grpcEndpoint"
    case certificateProviderInstance = "certificateProviderInstance"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)

    var type: OneOf_Type? = nil
    let typeCheckAndSet = {
      if type != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath, debugDescription: "Multiple values set for oneof 'type'"
          ))
      }
      type = $0
    }
    if let grpcEndpoint = try container.decodeIfPresent(GrpcEndpoint?.self, forKey: .grpcEndpoint) {
      try typeCheckAndSet(.grpcEndpoint(grpcEndpoint))
    }
    if let certificateProviderInstance = try container.decodeIfPresent(
      CertificateProviderInstance?.self, forKey: .certificateProviderInstance)
    {
      try typeCheckAndSet(.certificateProviderInstance(certificateProviderInstance))
    }
    self.type = type
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)

    if let choice = self.type {
      switch choice {
      case .grpcEndpoint(let value):
        try container.encode(value, forKey: .grpcEndpoint)
      case .certificateProviderInstance(let value):
        try container.encode(value, forKey: .certificateProviderInstance)
      }
    }
  }

  /// The type of certificate provider which provides the certificates and
  /// private keys.
  public enum OneOf_Type: Codable, Equatable, Sendable {
    /// gRPC specific configuration to access the gRPC server to
    /// obtain the cert and private key.
    indirect case grpcEndpoint(GrpcEndpoint?)
    /// The certificate provider instance specification that will be passed to
    /// the data plane, which will be used to load necessary credential
    /// information.
    indirect case certificateProviderInstance(CertificateProviderInstance?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networksecurity.v1.CertificateProvider"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
