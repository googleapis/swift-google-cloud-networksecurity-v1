// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// `AuthzPolicy` is a resource that allows to forward traffic to a
/// callout backend designed to scan the traffic for security purposes.
public struct AuthzPolicy: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Required. Identifier. Name of the `AuthzPolicy` resource in the following
  /// format:
  /// `projects/{project}/locations/{location}/authzPolicies/{authz_policy}`.
  public var name: Swift.String = Swift.String()

  /// Output only. The timestamp when the resource was created.
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. The timestamp when the resource was updated.
  public var updateTime: GoogleCloudWkt.Timestamp? = nil

  /// Optional. A human-readable description of the resource.
  public var description: Swift.String = Swift.String()

  /// Optional. Set of labels associated with the `AuthzPolicy` resource.
  ///
  /// The format must comply with [the following
  /// requirements](/compute/docs/labeling-resources#requirements).
  public var labels: [Swift.String: Swift.String] = [:]

  /// Required. Specifies the set of resources to which this policy should be
  /// applied to.
  public var target: AuthzPolicy.Target? = nil

  /// Optional. A list of authorization HTTP rules to match against the incoming
  /// request. A policy match occurs when at least one HTTP rule matches the
  /// request or when no HTTP rules are specified in the policy.
  /// At least one HTTP Rule is required for Allow or Deny Action. Limited
  /// to 5 rules.
  public var httpRules: [AuthzPolicy.AuthzRule] = []

  /// Optional. A list of authorization network rules to match against the
  /// incoming request. A policy match occurs when at least one network rule
  /// matches the request.
  /// At least one network rule is required for Allow or Deny Action if no HTTP
  /// rules are provided. Network rules are mutually exclusive with HTTP rules.
  /// Limited to 5 rules.
  public var networkRules: [AuthzPolicy.AuthzRule] = []

  /// Required. Can be one of `ALLOW`, `DENY`, `CUSTOM`.
  ///
  /// When the action is `CUSTOM`, `customProvider` must be specified.
  ///
  /// When the action is `ALLOW`, only requests matching the policy will
  /// be allowed.
  ///
  /// When the action is `DENY`, only requests matching the policy will be
  /// denied.
  ///
  /// When a request arrives, the policies are evaluated in the following order:
  ///
  /// 1. If there is a `CUSTOM` policy that matches the request, the `CUSTOM`
  /// policy is evaluated using the custom authorization providers and the
  /// request is denied if the provider rejects the request.
  ///
  /// 2. If there are any `DENY` policies that match the request, the request
  /// is denied.
  ///
  /// 3. If there are no `ALLOW` policies for the resource or if any of the
  /// `ALLOW` policies match the request, the request is allowed.
  ///
  /// 4. Else the request is denied by default if none of the configured
  /// AuthzPolicies with `ALLOW` action match the request.
  public var action: AuthzPolicy.AuthzAction = AuthzPolicy.AuthzAction()

  /// Optional. Required if the action is `CUSTOM`. Allows delegating
  /// authorization decisions to Cloud IAP or to Service Extensions. One of
  /// `cloudIap` or `authzExtension` must be specified.
  public var customProvider: AuthzPolicy.CustomProvider? = nil

  /// Optional. Immutable. Defines the type of authorization being performed.
  /// If not specified, `REQUEST_AUTHZ` is applied. This field cannot be changed
  /// once AuthzPolicy is created.
  public var policyProfile: AuthzPolicy.PolicyProfile = AuthzPolicy.PolicyProfile()

  /// Initialize a new instance of `AuthzPolicy`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = AuthzPolicy().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Specifies the set of targets to which this policy should be applied to.
  public struct Target: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. All gateways and forwarding rules referenced by this policy and
    /// extensions must share the same load balancing scheme. Required only when
    /// targeting forwarding rules. If targeting Secure Web Proxy, this field
    /// must be `INTERNAL_MANAGED` or not specified. Must not be specified
    /// when targeting Agent Gateway. Supported values:
    /// `INTERNAL_MANAGED` and `EXTERNAL_MANAGED`. For more information, refer
    /// to [Backend services
    /// overview](https://cloud.google.com/load-balancing/docs/backend-service).
    public var loadBalancingScheme: AuthzPolicy.LoadBalancingScheme =
      AuthzPolicy.LoadBalancingScheme()

    /// Required. A list of references to the Forwarding Rules, Secure Web Proxy
    /// Gateways, or Agent Gateways on which this policy will be applied.
    public var resources: [Swift.String] = []

    /// Initialize a new instance of `Target`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = Target().with { $0.loadBalancingScheme = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.Target"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Conditions to match against the incoming request.
  public struct AuthzRule: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. Describes properties of a source of a request.
    public var from: AuthzPolicy.AuthzRule.From? = nil

    /// Optional. Describes properties of a target of a request.
    public var to: AuthzPolicy.AuthzRule.To? = nil

    /// Optional. CEL expression that describes the conditions to be satisfied
    /// for the action. The result of the CEL expression is ANDed with the from
    /// and to. Refer to the CEL language reference for a list of available
    /// attributes.
    public var when: Swift.String = Swift.String()

    /// Initialize a new instance of `AuthzRule`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = AuthzRule().with { $0.from = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Determines how a string value should be matched.
    public struct StringMatch: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// If true, indicates the exact/prefix/suffix/contains matching should be
      /// case insensitive. For example, the matcher ``data`` will match both
      /// input string ``Data`` and ``data`` if set to true.
      public var ignoreCase: Swift.Bool = Swift.Bool()

      public var matchPattern: OneOf_MatchPattern? = nil

      /// Initialize a new instance of `StringMatch`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = StringMatch().with { $0.exact = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case exact = "exact"
        case `prefix` = "prefix"
        case suffix = "suffix"
        case contains = "contains"
        case ignoreCase = "ignoreCase"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.ignoreCase = try container.decode(Swift.Bool.self, forKey: .ignoreCase)

        var matchPattern: OneOf_MatchPattern? = nil
        let matchPatternCheckAndSet = {
          if matchPattern != nil {
            throw DecodingError.dataCorrupted(
              DecodingError.Context(
                codingPath: decoder.codingPath,
                debugDescription: "Multiple values set for oneof 'matchPattern'"))
          }
          matchPattern = $0
        }
        if let exact = try container.decodeIfPresent(Swift.String.self, forKey: .exact) {
          try matchPatternCheckAndSet(.exact(exact))
        }
        if let `prefix` = try container.decodeIfPresent(Swift.String.self, forKey: .`prefix`) {
          try matchPatternCheckAndSet(.`prefix`(`prefix`))
        }
        if let suffix = try container.decodeIfPresent(Swift.String.self, forKey: .suffix) {
          try matchPatternCheckAndSet(.suffix(suffix))
        }
        if let contains = try container.decodeIfPresent(Swift.String.self, forKey: .contains) {
          try matchPatternCheckAndSet(.contains(contains))
        }
        self.matchPattern = matchPattern
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.ignoreCase, forKey: .ignoreCase)

        if let choice = self.matchPattern {
          switch choice {
          case .exact(let value):
            try container.encode(value, forKey: .exact)
          case .`prefix`(let value):
            try container.encode(value, forKey: .`prefix`)
          case .suffix(let value):
            try container.encode(value, forKey: .suffix)
          case .contains(let value):
            try container.encode(value, forKey: .contains)
          }
        }
      }

      public enum OneOf_MatchPattern: Codable, Equatable, Sendable {
        /// The input string must match exactly the string specified here.
        ///
        /// Examples:
        ///
        /// * ``abc`` only matches the value ``abc``.
        case exact(Swift.String)
        /// The input string must have the prefix specified here.
        /// Note: empty prefix is not allowed, please use regex instead.
        ///
        /// Examples:
        ///
        /// * ``abc`` matches the value ``abc.xyz``
        case `prefix`(Swift.String)
        /// The input string must have the suffix specified here.
        /// Note: empty prefix is not allowed, please use regex instead.
        ///
        /// Examples:
        ///
        /// * ``abc`` matches the value ``xyz.abc``
        case suffix(Swift.String)
        /// The input string must have the substring specified here.
        /// Note: empty contains match is not allowed, please use regex instead.
        ///
        /// Examples:
        ///
        /// * ``abc`` matches the value ``xyz.abc.def``
        case contains(Swift.String)
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.StringMatch"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Represents a range of IP Addresses.
    public struct IpBlock: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. The address prefix.
      public var `prefix`: Swift.String = Swift.String()

      /// Required. The length of the address range.
      public var length: Swift.Int32 = Swift.Int32()

      /// Initialize a new instance of `IpBlock`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = IpBlock().with { $0.`prefix` = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      private enum CodingKeys: Swift.String, CodingKey {
        case `prefix` = "prefix"
        case length = "length"
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.`prefix` = try container.decode(Swift.String.self, forKey: .`prefix`)
        self.length = try container.decode(Swift.Int32.self, forKey: .length)
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.`prefix`, forKey: .`prefix`)
        try container.encode(self.length, forKey: .length)
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.IpBlock"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Describes the properties of a client VM resource accessing the internal
    /// application load balancers.
    public struct RequestResource: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. A list of resource tag value permanent IDs to match against
      /// the resource manager tags value associated with the source VM of a
      /// request.
      public var tagValueIdSet: AuthzPolicy.AuthzRule.RequestResource.TagValueIdSet? = nil

      /// Optional. An IAM service account to match against the source
      /// service account of the VM sending the request.
      public var iamServiceAccount: AuthzPolicy.AuthzRule.StringMatch? = nil

      /// Initialize a new instance of `RequestResource`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = RequestResource().with { $0.tagValueIdSet = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Describes a set of resource tag value permanent IDs to match against
      /// the resource manager tags value associated with the source VM of a
      /// request.
      public struct TagValueIdSet: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Required. A list of resource tag value permanent IDs to match against
        /// the resource manager tags value associated with the source VM of a
        /// request. The match follows AND semantics which means all
        /// the ids must match. Limited to 5 ids in the Tag value id set.
        public var ids: [Swift.Int64] = []

        /// Initialize a new instance of `TagValueIdSet`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = TagValueIdSet().with { $0.ids = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.RequestResource.TagValueIdSet"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.RequestResource"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Determines how a HTTP header should be matched.
    public struct HeaderMatch: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. Specifies the name of the header in the request.
      public var name: Swift.String = Swift.String()

      /// Optional. Specifies how the header match will be performed.
      public var value: AuthzPolicy.AuthzRule.StringMatch? = nil

      /// Initialize a new instance of `HeaderMatch`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = HeaderMatch().with { $0.name = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.HeaderMatch"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Describes the properties of a principal to be matched against.
    public struct Principal: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. An enum to decide what principal value the principal rule
      /// will match against. If not specified, the PrincipalSelector is
      /// CLIENT_CERT_URI_SAN.
      public var principalSelector: AuthzPolicy.AuthzRule.Principal.PrincipalSelector = AuthzPolicy
        .AuthzRule.Principal.PrincipalSelector()

      /// Required. A non-empty string whose value is matched against the
      /// principal value based on the principal_selector. Only exact match can
      /// be applied for CLIENT_CERT_URI_SAN, CLIENT_CERT_DNS_NAME_SAN,
      /// CLIENT_CERT_COMMON_NAME selectors.
      public var principal: AuthzPolicy.AuthzRule.StringMatch? = nil

      /// Initialize a new instance of `Principal`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = Principal().with { $0.principalSelector = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// The principal value the principal rule will match against.
      public enum PrincipalSelector: Codable, Equatable, Sendable {
        /// Unspecified principal selector. It will be treated as
        /// CLIENT_CERT_URI_SAN by default.
        case unspecified
        /// The principal rule is matched against a list of URI SANs in the
        /// validated client's certificate. A match happens when there is any
        /// exact URI SAN value match. This is the default principal selector.
        case clientCertUriSan
        /// The principal rule is matched against a list of DNS Name SANs in the
        /// validated client's certificate. A match happens when there is any
        /// exact DNS Name SAN value match.
        /// This is only applicable for Application Load Balancers
        /// except for classic Global External Application load balancer.
        /// CLIENT_CERT_DNS_NAME_SAN is not supported for INTERNAL_SELF_MANAGED
        /// load balancing scheme.
        case clientCertDnsNameSan
        /// The principal rule is matched against the common name in the client's
        /// certificate. Authorization against multiple common names in the
        /// client certificate is not supported. Requests with multiple common
        /// names in the client certificate will be rejected if
        /// CLIENT_CERT_COMMON_NAME is set as the principal selector. A match
        /// happens when there is an exact common name value match.
        /// This is only applicable for Application Load Balancers
        /// except for global external Application Load Balancer and
        /// classic Application Load Balancer.
        /// CLIENT_CERT_COMMON_NAME is not supported for INTERNAL_SELF_MANAGED
        /// load balancing scheme.
        case clientCertCommonName
        /// Encodes an unknown integer value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownIntValue(Int)
        /// Encodes an unknown string value.
        ///
        /// The most common cause for an unknown values is for the service to send
        /// a value unknown to the library. We recommend you update your library to
        /// the latest version.
        case unknownStringValue(String)

        public init() {
          self = .unspecified
        }

        /// Returns the integer value associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown string value, this returns `nil`.
        public var intValue: Int? {
          switch self {
          case .unspecified: return 0
          case .clientCertUriSan: return 1
          case .clientCertDnsNameSan: return 2
          case .clientCertCommonName: return 3
          case .unknownIntValue(let v): return v
          case .unknownStringValue: return nil
          }
        }

        /// Returns the string value (or name) associated with the enumeration.
        ///
        /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
        public var stringValue: Swift.String? {
          switch self {
          case .unspecified: return "PRINCIPAL_SELECTOR_UNSPECIFIED"
          case .clientCertUriSan: return "CLIENT_CERT_URI_SAN"
          case .clientCertDnsNameSan: return "CLIENT_CERT_DNS_NAME_SAN"
          case .clientCertCommonName: return "CLIENT_CERT_COMMON_NAME"
          case .unknownIntValue: return nil
          case .unknownStringValue(let v): return v
          }
        }

        /// Initialize from a string value.
        ///
        /// If the value is unknown, this initializes to [`unknownStringValue`](doc:PrincipalSelector/unknownStringValue(_:)).
        public init(stringValue: Swift.String) {
          switch stringValue {
          case "PRINCIPAL_SELECTOR_UNSPECIFIED": self = .unspecified
          case "CLIENT_CERT_URI_SAN": self = .clientCertUriSan
          case "CLIENT_CERT_DNS_NAME_SAN": self = .clientCertDnsNameSan
          case "CLIENT_CERT_COMMON_NAME": self = .clientCertCommonName
          default: self = .unknownStringValue(stringValue)
          }
        }

        /// Initialize from an integer value.
        ///
        /// If the value is unknown, this initializes to [`unknownIntValue`](doc:PrincipalSelector/unknownIntValue(_:)).
        public init(intValue: Int) {
          switch intValue {
          case 0: self = .unspecified
          case 1: self = .clientCertUriSan
          case 2: self = .clientCertDnsNameSan
          case 3: self = .clientCertCommonName
          default: self = .unknownIntValue(intValue)
          }
        }

        public init(from decoder: Decoder) throws {
          let container = try decoder.singleValueContainer()
          if let v = try? container.decode(Int.self) {
            self.init(intValue: v)
            return
          }
          if let s = try? container.decode(String.self) {
            if let v = Int(s) {
              self.init(intValue: v)
            } else {
              self.init(stringValue: s)
            }
            return
          }
          throw DecodingError.dataCorruptedError(
            in: container, debugDescription: "Expected enum value, must be integer or string.")
        }

        public func encode(to encoder: Encoder) throws {
          var container = encoder.singleValueContainer()
          switch self {
          case .unspecified: return try container.encode(0)
          case .clientCertUriSan: return try container.encode(1)
          case .clientCertDnsNameSan: return try container.encode(2)
          case .clientCertCommonName: return try container.encode(3)
          case .unknownIntValue(let v): return try container.encode(v)
          case .unknownStringValue(let v): return try container.encode(v)
          }
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.Principal"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Describes properties of one or more sources of a request.
    public struct From: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. Describes the properties of a request's sources. At least one
      /// of sources or notSources must be specified. Limited to 1 source.
      /// A match occurs when ANY source (in sources or notSources) matches the
      /// request. Within a single source, the match follows AND semantics
      /// across fields and OR semantics within a single field, i.e. a match
      /// occurs when ANY principal matches AND ANY ipBlocks match.
      public var sources: [AuthzPolicy.AuthzRule.From.RequestSource] = []

      /// Optional. Describes the negated properties of request sources. Matches
      /// requests from sources that do not match the criteria specified in this
      /// field. At least one of sources or notSources must be specified.
      public var notSources: [AuthzPolicy.AuthzRule.From.RequestSource] = []

      /// Initialize a new instance of `From`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = From().with { $0.sources = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Describes the properties of a single source.
      public struct RequestSource: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. A list of identities derived from the client's certificate.
        /// This field will not match on a request unless frontend mutual TLS is
        /// enabled for the forwarding rule or Gateway and the client certificate
        /// has been successfully validated by mTLS.
        /// Each identity is a string whose value is matched against a list of
        /// URI SANs, DNS Name SANs, or the common name in the client's
        /// certificate. A match happens when any principal matches with the
        /// rule. Limited to 50 principals per Authorization Policy for regional
        /// internal Application Load Balancers, regional external Application
        /// Load Balancers, cross-region internal Application Load Balancers, and
        /// Cloud Service Mesh. This field is not supported for global external
        /// Application Load Balancers.
        public var principals: [AuthzPolicy.AuthzRule.Principal] = []

        /// Optional. A list of IP addresses or IP address ranges to match
        /// against the source IP address of the request. Limited to 10 ip_blocks
        /// per Authorization Policy
        public var ipBlocks: [AuthzPolicy.AuthzRule.IpBlock] = []

        /// Optional. A list of resources to match against the resource of the
        /// source VM of a request. Limited to 10 resources per Authorization
        /// Policy.
        public var resources: [AuthzPolicy.AuthzRule.RequestResource] = []

        /// Initialize a new instance of `RequestSource`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = RequestSource().with { $0.principals = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.From.RequestSource"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.From"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Describes properties of one or more targets of a request.
    public struct To: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Optional. Describes properties of one or more targets of a request. At
      /// least one of operations or notOperations must be specified. Limited to
      /// 1 operation. A match occurs when ANY operation (in operations or
      /// notOperations) matches. Within an operation, the match follows AND
      /// semantics across fields and OR semantics within a field, i.e. a match
      /// occurs when ANY path matches AND ANY header matches and ANY method
      /// matches.
      public var operations: [AuthzPolicy.AuthzRule.To.RequestOperation] = []

      /// Optional. Describes the negated properties of the targets of a request.
      /// Matches requests for operations that do not match the criteria
      /// specified in this field. At least one of operations or notOperations
      /// must be specified.
      public var notOperations: [AuthzPolicy.AuthzRule.To.RequestOperation] = []

      /// Initialize a new instance of `To`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = To().with { $0.operations = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      /// Describes properties of one or more targets of a request.
      public struct RequestOperation: Codable, Equatable, GoogleCloudWkt._AnyPackable,
        Sendable
      {
        /// Optional. A list of headers to match against in http header.
        public var headerSet: AuthzPolicy.AuthzRule.To.RequestOperation.HeaderSet? = nil

        /// Optional. A list of HTTP Hosts to match against. The match can be one
        /// of exact, prefix, suffix, or contains (substring match). Matches are
        /// always case sensitive unless the ignoreCase is set. Limited to 10
        /// hosts per Authorization Policy.
        public var hosts: [AuthzPolicy.AuthzRule.StringMatch] = []

        /// Optional. A list of paths to match against. The match can be one of
        /// exact, prefix, suffix, or contains (substring match). Matches are
        /// always case sensitive unless the ignoreCase is set. Limited to 10
        /// paths per Authorization Policy.
        /// Note that this path match includes the query parameters. For gRPC
        /// services, this should be a fully-qualified name of the form
        /// /package.service/method.
        public var paths: [AuthzPolicy.AuthzRule.StringMatch] = []

        /// Optional. A list of HTTP methods to match against. Each entry must be
        /// a valid HTTP method name (GET, PUT, POST, HEAD, PATCH, DELETE,
        /// OPTIONS). It only allows exact match and is always case sensitive.
        /// Limited to 10 methods per Authorization Policy.
        public var methods: [Swift.String] = []

        /// Optional. Defines the MCP protocol attributes to match on. If the MCP
        /// payload in the request body cannot be successfully parsed, the
        /// request will be denied. This field can be set only for AuthzPolicies
        /// targeting AgentGateway resources.
        public var mcp: AuthzPolicy.AuthzRule.To.RequestOperation.MCP? = nil

        /// Optional. A list of SNIs to match against. The match can be one of
        /// exact, prefix, suffix, or contains (substring match). If there is no
        /// SNI (i.e. plaintext HTTP traffic), the request will be denied.
        /// Matches are always case sensitive unless the ignoreCase is set.
        /// Limited to 10 SNIs per Authorization Policy.
        public var snis: [AuthzPolicy.AuthzRule.StringMatch] = []

        /// Initialize a new instance of `RequestOperation`.
        public init() {}

        /// Use `config` to return a new instance of this object, with some fields updated.
        ///
        /// Commonly used to initialize the value, for example:
        ///
        /// ```
        /// let value = RequestOperation().with { $0.headerSet = ... }
        /// ```
        public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
          var copy = self
          try config(&copy)
          return copy
        }

        /// Describes a set of HTTP headers to match against.
        public struct HeaderSet: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Required. A list of headers to match against in http header.
          /// The match can be one of exact, prefix, suffix, or contains
          /// (substring match). The match follows AND semantics which means all
          /// the headers must match. Matches are always case sensitive unless
          /// the ignoreCase is set. Limited to 10 headers per Authorization
          /// Policy.
          public var headers: [AuthzPolicy.AuthzRule.HeaderMatch] = []

          /// Initialize a new instance of `HeaderSet`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = HeaderSet().with { $0.headers = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.To.RequestOperation.HeaderSet"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Describes a set of MCP methods to match against.
        public struct MCPMethod: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Required. The MCP method to match against. Allowed values are as
          /// follows:
          /// 1. `tools`, `prompts`, `resources` - these will match against all
          ///    sub methods under the respective methods.
          /// 2. `prompts/list`, `tools/list`, `resources/list`,
          ///    `resources/templates/list`
          /// 3. `prompts/get`, `tools/call`, `resources/subscribe`,
          ///    `resources/unsubscribe`, `resources/read`
          /// Params cannot be specified for categories 1 and 2.
          public var name: Swift.String = Swift.String()

          /// Optional. A list of MCP method parameters to match against. The
          /// match can be one of exact, prefix, suffix, or contains (substring
          /// match). Matches are always case sensitive unless the ignoreCase is
          /// set. Limited to 10 MCP method parameters per Authorization Policy.
          public var params: [AuthzPolicy.AuthzRule.StringMatch] = []

          /// Initialize a new instance of `MCPMethod`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = MCPMethod().with { $0.name = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.To.RequestOperation.MCPMethod"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Describes a set of MCP protocol attributes to match against for a
        /// given MCP request.
        public struct MCP: Codable, Equatable, GoogleCloudWkt._AnyPackable,
          Sendable
        {
          /// Optional. If specified, matches on the MCP protocol’s non-access
          /// specific methods namely:
          /// * initialize
          /// * completion/
          /// * logging/
          /// * notifications/
          /// * ping
          /// Defaults to SKIP_BASE_PROTOCOL_METHODS if not specified.
          public var baseProtocolMethodsOption:
            AuthzPolicy.AuthzRule.To.RequestOperation.BaseProtocolMethodsOption = AuthzPolicy
              .AuthzRule.To.RequestOperation.BaseProtocolMethodsOption()

          /// Optional. A list of MCP methods and associated parameters to match
          /// on. It is recommended to use this field to match on tools, prompts
          /// and resource accesses while setting the baseProtocolMethodsOption
          /// to MATCH_BASE_PROTOCOL_METHODS to match on all the other MCP
          /// protocol methods.
          /// Limited to 10 MCP methods per Authorization Policy.
          public var methods: [AuthzPolicy.AuthzRule.To.RequestOperation.MCPMethod] = []

          /// Initialize a new instance of `MCP`.
          public init() {}

          /// Use `config` to return a new instance of this object, with some fields updated.
          ///
          /// Commonly used to initialize the value, for example:
          ///
          /// ```
          /// let value = MCP().with { $0.baseProtocolMethodsOption = ... }
          /// ```
          public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
            var copy = self
            try config(&copy)
            return copy
          }

          public static var _anyTypeUrl: Swift.String {
            return
              "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.To.RequestOperation.MCP"
          }
          public init(fromAny any: GoogleCloudWkt.`Any`) throws {
            self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
          }
          public func _pack() throws -> GoogleCloudWkt.Struct {
            return try GoogleCloudWkt._slowAnySerialize(message: self)
          }
        }

        /// Describes the option to match against the base MCP protocol methods.
        public enum BaseProtocolMethodsOption: Codable, Equatable, Sendable {
          /// Unspecified option. Defaults to SKIP_BASE_PROTOCOL_METHODS.
          case unspecified
          /// Skip matching on the base MCP protocol methods.
          case skipBaseProtocolMethods
          /// Match on the base MCP protocol methods.
          case matchBaseProtocolMethods
          /// Encodes an unknown integer value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownIntValue(Int)
          /// Encodes an unknown string value.
          ///
          /// The most common cause for an unknown values is for the service to send
          /// a value unknown to the library. We recommend you update your library to
          /// the latest version.
          case unknownStringValue(String)

          public init() {
            self = .unspecified
          }

          /// Returns the integer value associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown string value, this returns `nil`.
          public var intValue: Int? {
            switch self {
            case .unspecified: return 0
            case .skipBaseProtocolMethods: return 1
            case .matchBaseProtocolMethods: return 2
            case .unknownIntValue(let v): return v
            case .unknownStringValue: return nil
            }
          }

          /// Returns the string value (or name) associated with the enumeration.
          ///
          /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
          public var stringValue: Swift.String? {
            switch self {
            case .unspecified: return "BASE_PROTOCOL_METHODS_OPTION_UNSPECIFIED"
            case .skipBaseProtocolMethods: return "SKIP_BASE_PROTOCOL_METHODS"
            case .matchBaseProtocolMethods: return "MATCH_BASE_PROTOCOL_METHODS"
            case .unknownIntValue: return nil
            case .unknownStringValue(let v): return v
            }
          }

          /// Initialize from a string value.
          ///
          /// If the value is unknown, this initializes to [`unknownStringValue`](doc:BaseProtocolMethodsOption/unknownStringValue(_:)).
          public init(stringValue: Swift.String) {
            switch stringValue {
            case "BASE_PROTOCOL_METHODS_OPTION_UNSPECIFIED": self = .unspecified
            case "SKIP_BASE_PROTOCOL_METHODS": self = .skipBaseProtocolMethods
            case "MATCH_BASE_PROTOCOL_METHODS": self = .matchBaseProtocolMethods
            default: self = .unknownStringValue(stringValue)
            }
          }

          /// Initialize from an integer value.
          ///
          /// If the value is unknown, this initializes to [`unknownIntValue`](doc:BaseProtocolMethodsOption/unknownIntValue(_:)).
          public init(intValue: Int) {
            switch intValue {
            case 0: self = .unspecified
            case 1: self = .skipBaseProtocolMethods
            case 2: self = .matchBaseProtocolMethods
            default: self = .unknownIntValue(intValue)
            }
          }

          public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            if let v = try? container.decode(Int.self) {
              self.init(intValue: v)
              return
            }
            if let s = try? container.decode(String.self) {
              if let v = Int(s) {
                self.init(intValue: v)
              } else {
                self.init(stringValue: s)
              }
              return
            }
            throw DecodingError.dataCorruptedError(
              in: container, debugDescription: "Expected enum value, must be integer or string.")
          }

          public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            switch self {
            case .unspecified: return try container.encode(0)
            case .skipBaseProtocolMethods: return try container.encode(1)
            case .matchBaseProtocolMethods: return try container.encode(2)
            case .unknownIntValue(let v): return try container.encode(v)
            case .unknownStringValue(let v): return try container.encode(v)
            }
          }
        }

        public static var _anyTypeUrl: Swift.String {
          return
            "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.To.RequestOperation"
        }
        public init(fromAny any: GoogleCloudWkt.`Any`) throws {
          self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
        }
        public func _pack() throws -> GoogleCloudWkt.Struct {
          return try GoogleCloudWkt._slowAnySerialize(message: self)
        }
      }

      public static var _anyTypeUrl: Swift.String {
        return "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule.To"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.AuthzRule"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Allows delegating authorization decisions to Cloud IAP or to
  /// Service Extensions.
  public struct CustomProvider: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Optional. Delegates authorization decisions to Cloud IAP. Applicable
    /// only for managed load balancers. Enabling Cloud IAP at the AuthzPolicy
    /// level is not compatible with Cloud IAP settings in the BackendService.
    /// Enabling IAP in both places will result in request failure. Ensure that
    /// IAP is enabled in either the AuthzPolicy or the BackendService but not in
    /// both places.
    public var cloudIap: AuthzPolicy.CustomProvider.CloudIap? = nil

    /// Optional. Delegate authorization decision to user authored Service
    /// Extension. Only one of cloudIap or authzExtension can be specified.
    public var authzExtension: AuthzPolicy.CustomProvider.AuthzExtension? = nil

    /// Initialize a new instance of `CustomProvider`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CustomProvider().with { $0.cloudIap = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Optional. Delegates authorization decisions to Cloud IAP. Applicable
    /// only for managed load balancers. Enabling Cloud IAP at the AuthzPolicy
    /// level is not compatible with Cloud IAP settings in the BackendService.
    /// Enabling IAP in both places will result in request failure. Ensure that
    /// IAP is enabled in either the AuthzPolicy or the BackendService but not in
    /// both places.
    public struct CloudIap: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Initialize a new instance of `CloudIap`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = CloudIap().with { $0.<placeholder> = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.CustomProvider.CloudIap"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    /// Optional. Delegate authorization decision to user authored extension.
    /// Only one of cloudIap or authzExtension can be specified.
    public struct AuthzExtension: Codable, Equatable, GoogleCloudWkt._AnyPackable,
      Sendable
    {
      /// Required. A list of references to authorization
      /// extensions that will be invoked for requests matching this policy.
      /// Limited to 1 custom provider.
      public var resources: [Swift.String] = []

      /// Initialize a new instance of `AuthzExtension`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = AuthzExtension().with { $0.resources = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.CustomProvider.AuthzExtension"
      }
      public init(fromAny any: GoogleCloudWkt.`Any`) throws {
        self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWkt.Struct {
        return try GoogleCloudWkt._slowAnySerialize(message: self)
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy.CustomProvider"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Load balancing schemes supported by the `AuthzPolicy` resource. The valid
  /// values are `INTERNAL_MANAGED` and
  /// `EXTERNAL_MANAGED`. For more information, refer to [Backend services
  /// overview](https://cloud.google.com/load-balancing/docs/backend-service).
  public enum LoadBalancingScheme: Codable, Equatable, Sendable {
    /// Default value. Do not use.
    case unspecified
    /// Signifies that this is used for Regional internal or Cross-region
    /// internal Application Load Balancing.
    case internalManaged
    /// Signifies that this is used for Global external or Regional external
    /// Application Load Balancing.
    case externalManaged
    /// Signifies that this is used for Cloud Service Mesh. Meant for use by
    /// CSM GKE controller only.
    case internalSelfManaged
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .internalManaged: return 1
      case .externalManaged: return 2
      case .internalSelfManaged: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "LOAD_BALANCING_SCHEME_UNSPECIFIED"
      case .internalManaged: return "INTERNAL_MANAGED"
      case .externalManaged: return "EXTERNAL_MANAGED"
      case .internalSelfManaged: return "INTERNAL_SELF_MANAGED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:LoadBalancingScheme/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "LOAD_BALANCING_SCHEME_UNSPECIFIED": self = .unspecified
      case "INTERNAL_MANAGED": self = .internalManaged
      case "EXTERNAL_MANAGED": self = .externalManaged
      case "INTERNAL_SELF_MANAGED": self = .internalSelfManaged
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:LoadBalancingScheme/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .internalManaged
      case 2: self = .externalManaged
      case 3: self = .internalSelfManaged
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .internalManaged: return try container.encode(1)
      case .externalManaged: return try container.encode(2)
      case .internalSelfManaged: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The action to be applied to this policy. Valid values are
  /// `ALLOW`, `DENY`, `CUSTOM`.
  public enum AuthzAction: Codable, Equatable, Sendable {
    /// Unspecified action.
    case unspecified
    /// Allow request to pass through to the backend.
    case allow
    /// Deny the request and return a HTTP 404 to the client.
    case deny
    /// Delegate the authorization decision to an external authorization engine.
    case custom
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .allow: return 1
      case .deny: return 2
      case .custom: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "AUTHZ_ACTION_UNSPECIFIED"
      case .allow: return "ALLOW"
      case .deny: return "DENY"
      case .custom: return "CUSTOM"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:AuthzAction/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "AUTHZ_ACTION_UNSPECIFIED": self = .unspecified
      case "ALLOW": self = .allow
      case "DENY": self = .deny
      case "CUSTOM": self = .custom
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:AuthzAction/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .allow
      case 2: self = .deny
      case 3: self = .custom
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .allow: return try container.encode(1)
      case .deny: return try container.encode(2)
      case .custom: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The type of authorization being performed.
  /// New values may be added in the future.
  public enum PolicyProfile: Codable, Equatable, Sendable {
    /// Unspecified policy profile.
    case unspecified
    /// Applies to request authorization. `CUSTOM` authorization
    /// policies with Authz extensions will be allowed with `EXT_AUTHZ_GRPC` or
    /// `EXT_PROC_GRPC` protocols. Extensions are invoked only for request header
    /// events.
    case requestAuthz
    /// Applies to content security, sanitization, etc. Only
    /// `CUSTOM` action is allowed in this policy profile. AuthzExtensions in the
    /// custom provider must support `EXT_PROC_GRPC` protocol only and be capable
    /// of receiving all `EXT_PROC_GRPC` events (REQUEST_HEADERS, REQUEST_BODY,
    /// REQUEST_TRAILERS, RESPONSE_HEADERS, RESPONSE_BODY, RESPONSE_TRAILERS)
    /// with `FULL_DUPLEX_STREAMED` body send mode.
    case contentAuthz
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .requestAuthz: return 1
      case .contentAuthz: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "POLICY_PROFILE_UNSPECIFIED"
      case .requestAuthz: return "REQUEST_AUTHZ"
      case .contentAuthz: return "CONTENT_AUTHZ"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:PolicyProfile/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "POLICY_PROFILE_UNSPECIFIED": self = .unspecified
      case "REQUEST_AUTHZ": self = .requestAuthz
      case "CONTENT_AUTHZ": self = .contentAuthz
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:PolicyProfile/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .requestAuthz
      case 2: self = .contentAuthz
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .requestAuthz: return try container.encode(1)
      case .contentAuthz: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.networksecurity.v1.AuthzPolicy"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
